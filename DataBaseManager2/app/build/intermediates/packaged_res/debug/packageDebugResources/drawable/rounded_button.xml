<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/yellow" />
    <corners android:radius="20dp" /> <!-- Закругленные углы -->
    <stroke
        android:width="2dp"
        android:color="@android:color/black" /> <!-- Черная рамка -->
</shape>
